name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    name: Test and build
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        env:
          DATABASE_URL: mysql://ci:ci@127.0.0.1:3306/okwook_ci_metadata
        run: npm ci

      - name: Run no-call tests
        run: npm run test

      - name: Build service
        run: npm run build:service

      - name: Build frontend
        run: npm run build
